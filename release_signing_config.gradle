android {
    signingConfigs {
        if ('jenkins'=="$System.env.USER")
        releaseConfig {
            storeFile file("$System.env.HOME/.android/debug.keystore")
            storePassword 'android'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
    }
}
